# backbone-app
```
sudo npm install -g jade
sudo npm install -g browser-sync

npm install

terminal #1
browser-sync start --server --port 3001 --files="./*"

terminal #2
jade --watch --pretty src/ index.jade

```

